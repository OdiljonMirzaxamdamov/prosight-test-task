
# Prosight DEV Test Task

Backend API test task implemented with NestJS and TypeScript.

## Overview

This project provides:

- JWT authentication with 3 predefined users:
  - `admin`
  - `normal`
  - `limited`
- `GET /locus` endpoint
- filtering, sorting, and pagination
- optional sideloading of locus members
- role-based access control
- autogenerated OpenAPI documentation via Swagger
- basic automated tests

## Tech Stack

- NestJS
- TypeScript
- TypeORM
- PostgreSQL
- JWT / Passport
- Swagger / OpenAPI
- Jest / Supertest

## API Features

### Authentication

The API uses JWT authentication.

Predefined users:
- `admin` / `prosightAdmin111`
- `normal` / `prosightNormal222`
- `limited` / `prosightLimited333`

Login endpoint:
POST /auth/login

Example request body:
{
  "username": "admin",
  "password": "prosightAdmin111"
}

### Locus endpoint

GET /locus

Supported query parameters:

- `id` — integer
- `assemblyId` — string
- `regionId` — integer
- `membershipStatus` — string
- `sideload` — enum: `locusMembers`
- `page` — integer, default `1`
- `limit` — integer, default `1000`, max `1000`
- `sortBy` — one of:
  - `id`
  - `assemblyId`
  - `locusStart`
  - `locusStop`
  - `memberCount`
- `sortOrder` — `ASC` or `DESC`

Example requests:

GET /locus
GET /locus?id=155095
GET /locus?regionId=31232818
GET /locus?sortBy=memberCount&sortOrder=DESC&limit=20
GET /locus?sideload=locusMembers&page=1&limit=10

## Access Rules

### Admin user

- can access all returned fields
- can use sideloading
- can access all data

### Normal user

- can access only data from `rl` table
- cannot use sideloading

### Limited user

- can access data only for the following `regionId` values:
  - 31232818
  - 41442807
  - 41447718
- cannot use sideloading

## Database

Datasource used in this task:

Host: hh-pgsql-public.ebi.ac.uk  
Port: 5432  
Database: pfmegrnargs  

The implementation uses ORM entities and relations without raw SQL.

## Project Structure

src/
  auth/
  common/
  locus/
  users/
  app.module.ts
  main.ts

## Environment Variables

Create a `.env` file in the project root:

PORT=3000

DB_HOST=hh-pgsql-public.ebi.ac.uk
DB_PORT=5432
DB_NAME=pfmegrnargs
DB_USER=reader
DB_PASSWORD=NWDMCE5xdipIjRrp

JWT_SECRET=super_secret_jwt_key
JWT_EXPIRES_IN=1d

Example template is also available in `.env.example`.

## Installation

npm install

## Running the project

Development mode:

npm run start:dev

Production build:

npm run build
npm run start

## Swagger Documentation

After starting the project, Swagger is available at:

http://localhost:3000/docs

## Running tests

Unit / default tests:

npm run test

End-to-end tests:

npm run test:e2e

## Notes and Assumptions

- `assemblyId` was implemented according to the actual datasource schema type.
- API response fields use `camelCase`, while database columns use `snake_case`.
- For filtering by `regionId` and `membershipStatus`, ORM relations are used.
- No raw SQL was used in the implementation.

## Author

Test task completed by Odiljon Mirzaxamdamov.
